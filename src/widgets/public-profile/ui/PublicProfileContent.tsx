import React from 'react'

import { UserProfileInfo } from '@/src/entities/profile/ui/PublicProfile/UserProfileInfo'

export const PublicProfileContent = () => {
  return (
    <div>
      <UserProfileInfo className={} myProfile={} ownerId={} userData={} />
    </div>
  )
}
